import { DefaultTheme } from "styled-components";
import { CBlack, CBlue1, CWhite } from "./colors";

export const lightTheme: DefaultTheme = {
  bgColor: CWhite,
  textColor: CBlack,
  accentColor: CBlue1,
};

export const darkTheme = {
  bgColor: CBlack,
  textColor: CWhite,
  accentColor: CBlue1,
};
